
# 32-byte key used to encrypt stored third-party secrets (payment gateway keys).
# Generate with: openssl rand -hex 32
# Without it the app still reads existing values, but refuses to store new secrets.
SETTINGS_ENCRYPTION_KEY=
